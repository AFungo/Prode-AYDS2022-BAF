<!DOCTYPE html>
<html>
  <head>
    <title>Predictions</title>
  </head>
  <body>
    <h1>Mis predicciones</h1>
    <% @predictions.each do |p| %>
        <%m = Match.find_by(id: p.match_id)%>
        <%= Team.find_by(id: m.local_id).name %>
        <%= p.team1_goals %>
        <%= 'vs' %> 
        <%= Team.find_by(id: m.visitor_id).name %>
        <%= p.team2_goals %>
        <%= ' Ronda: '%>
        <%= m.round%>
        </br>
    <% end %>
  </body>
</html>